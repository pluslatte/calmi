import { describe, it } from "vitest";

describe('when deleteAccount is called', () => {
    describe('success case', () => {
        it('isDeleting is properly updated', () => {
            // fill 
        });

        it('onAccountDeleted is called once on success', () => {
            // fill 
        })
    })

    describe('failure case', () => {
        it('isDeleting is properly updated', () => {
            // fill 
        });

        it('onAccountDeleted is NOT called', () => {
            // fill 
        });
    });
});

describe('returns', () => {
    it('isDeleting and deleteAccount', () => {
        // fill
    });

    it('isDeleting is boolean', () => {
        // fill
    });

    it('deleteAccount is () => void', () => {
        // fill
    });
});